const config = {
    routes: {
        stats: '/',
        allJobs: '/all-jobs',
        addJobs: '/add-job',
        profile: '/profile',
        landing: '/landing',
        register: '/register',
    },
};

export default config;
